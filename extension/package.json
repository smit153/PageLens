{
  "name": "pagelens-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n popup,background,content \"vite build --watch --mode development\" \"TARGET=background vite build --config vite.mv3.config.ts --watch --mode development\" \"TARGET=content vite build --config vite.mv3.config.ts --watch --mode development\"",
    "build": "vite build && TARGET=background vite build --config vite.mv3.config.ts && TARGET=content vite build --config vite.mv3.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@langchain/core": "^1.2.12",
    "@langchain/textsplitters": "^1.0.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "@types/node": "^26.6.2",
    "concurrently": "^10.0.5",
    "typescript": "^6.0.3",
    "vite": "^8.3.0"
  }
}

{
  "name": "pagelens-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env.local dev-server.ts",
    "typecheck": "tsc --noEmit",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.1.0",
    "@upstash/redis": "^1.38.4",
    "ai": "^7.0.107"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vercel": "^59.23.2"
  }
}

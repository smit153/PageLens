{
  "name": "pagelens-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "ai": "^7.0.107"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vercel": "^59.23.2"
  }
}
